# Save Local Reading History Without Audio

Reader's macOS app will save local Reading History so the user can review previously spoken text in reverse chronological order, similar to Typeless's history view. Each record preserves the full Reading Target text and metadata needed for time-based grouping and retention, but Reader does not save generated audio; this deliberately narrows persistence to text history while keeping speech output ephemeral.
