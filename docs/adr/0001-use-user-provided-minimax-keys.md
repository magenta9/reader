# Use User-Provided MiniMax Keys

Reader starts as a personal-use browser extension, so users provide their own MiniMax API key in the extension settings and the extension calls MiniMax directly. This avoids building account, quota, billing, and proxy infrastructure before the core reading experience is validated; a hosted proxy can be introduced later if Reader becomes a shared or public product.
