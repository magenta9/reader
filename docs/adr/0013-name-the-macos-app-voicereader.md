# Name the macOS App VoiceReader

The macOS app's product name is `VoiceReader`. User-facing window titles, menu labels, and packaging names should use `VoiceReader`; older `Reader` references remain historical context from the Chrome extension implementation unless they are intentionally migrated during desktop app work.
