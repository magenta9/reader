# Use React and TypeScript for the Electron UI

VoiceReader's Electron renderer will use React and TypeScript for the main window, settings, history detail views, and Playback Overlay UI. The desktop migration should remove obsolete Chrome extension code instead of keeping unused Manifest V3 surfaces alongside the Electron app.
