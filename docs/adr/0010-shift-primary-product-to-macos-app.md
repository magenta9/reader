---
status: accepted
---

# Shift Primary Product to macOS App

Reader's primary product direction is now a macOS menu bar app rather than the Chrome extension. The existing extension remains historical implementation context, but future product decisions should optimize for clipboard-based macOS reading, global shortcuts, local Reading History, and desktop settings instead of browser page extraction.
