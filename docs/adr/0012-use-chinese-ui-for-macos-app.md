# Use Chinese UI for the macOS App

Reader's macOS app will use Chinese for user-facing navigation, settings, controls, empty states, and history labels. The domain glossary and code-facing names remain in English so implementation terms stay stable while the product interface matches the user's preferred language.
